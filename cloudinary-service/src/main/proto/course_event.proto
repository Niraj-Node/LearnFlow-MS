syntax = "proto3";
package course.events;

message CourseEdited {
  string courseId = 1;
  string oldThumbnailUrl = 2;
  bytes NewImageData = 3;
}

message CourseThumbnailUploaded {
  string courseId = 1;
  string newThumbnailUrl = 2;
}